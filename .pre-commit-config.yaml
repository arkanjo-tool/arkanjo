exclude: ^(third-party/|archive/)

repos:
  - repo: local
    hooks:
      - id: git-clang-format
        name: git-clang-format
        entry: git-clang-format
        language: system
        args:
          - --staged
          - --extensions
          - cpp,hpp,h,cc,cxx
        pass_filenames: false
